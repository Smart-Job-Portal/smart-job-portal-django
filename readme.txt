Job Portal Advanced
پروژه پرتال کاریابی پیشرفته با استفاده از Django

پیش‌نیازها
Python 3.10 یا بالاتر
pip
virtualenv (اختیاری اما توصیه‌شده)
Git
نصب
مخزن را کلون کنید:

یک محیط مجازی بسازید و فعال کنید:

نصب وابستگی‌ها:

مهاجرت دیتابیس:

ساخت ابرکاربر (اختیاری):

اجرا
برای اجرای سرور توسعه:

سپس به آدرس http://127.0.0.1:8000 مراجعه کنید.

تست
برای اجرای تست‌ها:

استقرار
روی سرور لینوکس (مثلاً Ubuntu)
نصب پیش‌نیازها:

Python، pip، virtualenv، Git
نصب و راه‌اندازی پایگاه داده (مثلاً PostgreSQL یا MySQL)
کلون پروژه و نصب وابستگی‌ها (مانند مراحل نصب بالا)

تنظیم متغیرهای محیطی و فایل settings.py برای تولید (DEBUG=False، تنظیم SECRET_KEY و ...)

جمع‌آوری فایل‌های استاتیک:

اجرای مهاجرت‌ها:

راه‌اندازی با WSGI و سرویس‌دهنده (مانند Gunicorn و Nginx):

اجرای Gunicorn:
تنظیم Nginx برای پروکسی کردن درخواست‌ها به Gunicorn
استقرار با Docker (اختیاری)
اگر فایل Docker دارید:

